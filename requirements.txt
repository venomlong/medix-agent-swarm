# LLM和模型
openai>=1.33.0,<2.0.0
sentence-transformers==5.3.0
torch>=2.0.0
transformers>=4.41.0

# Agent框架
pydantic==2.10.0
pydantic-settings==2.7.0

# 记忆管理
mem0ai==0.1.0
redis==5.2.0
qdrant-client==1.12.0

# 向量数据库（Milvus）
pymilvus>=2.3.0
# milvus-lite 本应作为 pymilvus[milvus_lite] 的可选依赖自动安装，
# 但 pymilvus 的 extras 元数据里用 sys_platform != "win32" 排除了 Windows，
# 导致 Windows 上装不上，所以这里单独列出直接安装（milvus-lite 本身已支持 Windows）
milvus-lite>=2.4.0
# sentence-transformers 已在上方定义

# 搜索和研究
requests==2.32.0
beautifulsoup4==4.12.3
duckduckgo-search==6.3.5

# 数据处理
pandas>=2.0.0
numpy>=1.24.0
# 混合检索（BM25 + RRF）
rank-bm25>=0.2.2
jieba>=0.42.1

# 工具
loguru==0.7.3
python-dotenv==1.0.1
pyyaml==6.0.3
tqdm>=4.65.0

# 异步支持
aiohttp>=3.8.0
httpx>=0.24.0

# Web API（薄服务层，SSE）
fastapi>=0.115.0,<0.117.0
uvicorn[standard]>=0.32.0,<0.35.0
python-multipart>=0.0.20

# 测试（mock LLM 单测，不打真实 API）
pytest>=8.0.0
pytest-asyncio>=0.24.0
